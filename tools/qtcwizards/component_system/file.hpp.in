%{JS: Cpp.licenseTemplate()}\
@if '%{JS: Cpp.usePragmaOnce()}' === 'true'
#pragma once
@else
%{#: I don't know if this GUARD thing is actually defined. Whatever}
#ifndef %{GUARD}
#define %{GUARD}
@endif

@if %{HasCollisionTile}
#include <cinttypes>

@endif
@if %{HasCollision} || %{HasCollisionTile}
#include <cog2d/ecs/builtins/collisionsystem.hpp>

@endif
struct %{Component} {
};

struct Entity;
namespace systems {

void %{System}_construct(Entity& ent);
@if %{HasDraw}
void %{System}_draw(Entity& ent);
@endif
@if %{HasUpdate}
void %{System}_update(Entity& ent);
@endif
@if %{HasActivate}
void %{System}_activate(Entity& ent);
@endif
@if %{HasDeactivate}
void %{System}_deactivate(Entity& ent);
@endif
@if %{HasCollision}
cog2d::CollisionResponse %{System}_collision(Entity& ent, Entity& other);
@endif
@if %{HasCollisionTile}
cog2d::CollisionResponse %{System}_collision_tile(Entity& ent, std::size_t tileidx);
@endif

}

@if '%{JS: Cpp.usePragmaOnce()}' === 'false'
#endif // %{GUARD}
@endif
